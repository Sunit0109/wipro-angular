import { LifecycleExampleDirective } from './lifecycle-example.directive';

describe('LifecycleExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new LifecycleExampleDirective();
    expect(directive).toBeTruthy();
  });
});
